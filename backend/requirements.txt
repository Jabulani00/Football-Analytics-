# Scoreline backend — Python 3.10+ (3.12 recommended)
# Install: npm run install:backend   (from repo root)
#      or: python -m pip install -r requirements.txt

numpy>=1.26.0
pandas>=2.1.0
joblib>=1.3.0

# Prediction engine service (backend/prediction/).
# The pure core (poisson/features/ensemble/confidence/engine) needs NO deps —
# run `python -m prediction.demo` and the tests with stdlib only. These are for
# the live API + HTTP client layers:
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
httpx>=0.27.0
pydantic>=2.6.0

# Optional — ML training & TF.js export (pip install separately):
# tensorflow>=2.15.0
# tensorflowjs>=4.15.0
# chromadb>=0.4.0
# scikit-learn>=1.4.0   # Phase 1 ML upgrade (logistic regression per market)
# xgboost>=2.0.0        # Phase 2 ML upgrade
