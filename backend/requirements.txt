# Scoreline backend — Python 3.10+
# Install: npm run install:backend   (from repo root)
#      or: python -m pip install -r requirements.txt

numpy>=1.26.0
pandas>=2.1.0
joblib>=1.3.0

# Optional — ML training & TF.js export (pip install separately):
# tensorflow>=2.15.0
# tensorflowjs>=4.15.0
# chromadb>=0.4.0
